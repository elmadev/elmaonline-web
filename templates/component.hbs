import React from 'react';
import styled from 'styled-components';

const {{name}} = ({ children, to }) => {
  return <PageLink href={to}>{children}</PageLink>;
};

const PageLink = styled.div`
  color: #61dafb;
`;

export default {{name}};
